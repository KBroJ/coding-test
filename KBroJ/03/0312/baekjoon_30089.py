'''
[새로운 문자열 만들기]_https://www.acmicpc.net/problem/30089
-----------------------------------------------------------------------
T개의 줄마다 영어 대문자로만 이루어진 문자열 S가 주어질 때,
각 줄마다 아래 조건을 모두 만족하는 문자열 X를 출력하여라.

    X는 S로 시작하여야 한다.
    X를 뒤에서부터 읽은 문자열 X'에 대해서도 S로 시작하여야 한다.
    위 조건을 만족하는 문자열이 여러 가지라면, 가장 길이가 짧은 문자열이 X가 된다.
    가능한 모든 S에 대해서 조건을 만족하는 X는 유일함을 증명할 수 있다.
-----------------------------------------------------------------------
첫 번째 줄에 주어지는 문자열의 개수 T가 주어진다.(1 <= T <= 100)

두 번째 줄부터 T개의 줄에 걸쳐 문자열 S가 주어진다.
각 줄마다 주어지는 문자열 S의 길이는 1이상 20이하이다.
-----------------------------------------------------------------------
예제 입력
    3
    KITPA
    BANANA
    ROTATOR

예제 출력
    KITPAPTIK
    BANANAB
    ROTATOR
========================================================================================================================
생각하기
    입력값
        T : 숫자, S : 문자열
    출력값
        X : 문자열

    입력 문자열 S <= 20 : 완전탐색, 반복 다 가능

    X  : S
    X' : S 역순정렬

    겹치는 최대 길이 찾기
'''
import sys
input = sys.stdin.readline

def method():

    T = int(input())

    for i in range(T):
        S = input().strip() # .strip() : 줄바꿈(/n) 제거
        R = S[::-1]    # 역순정렬

        max_overlap = 0
        for a in range(1, len(S) + 1):
            if S.endswith(R[:a]):
                max_overlap = a

        X = S + R[max_overlap:]
        print(X)
method()
# ========================================================================================================================
'''
정리

    [알고리즘]: 문자열 처리 - 접미사/접두사 겹침(overlap) 탐색

    [풀이 방법]:
        1. R = S를 뒤집은 문자열 (reverse)
        2. "X가 S로 시작하고 X'도 S로 시작" = "X는 S로 시작하고 R로 끝나야 함"
        3. S의 끝부분과 R의 앞부분이 겹치는 최대 길이(max_overlap) 탐색
           → for a in range(1, len(S)+1): S.endswith(R[:a]) 이면 max_overlap 갱신
        4. X = S + R[max_overlap:]  (겹치는 부분 제외하고 R을 뒤에 붙임)

    [이상적인 풀이]:
        조건을 보고 "X는 R로 끝나야 함"을 먼저 파악하는 것이 핵심.
        S == R (이미 팰린드롬) 이면 max_overlap = len(S) → R[len(S):] = "" → X = S 그대로.
        현재 풀이가 이미 이 케이스를 자연스럽게 처리함.

        한 줄로 줄이면:
        next(S + R[i:] for i in range(len(S)+1) if S.endswith(R[:i]) or i==len(S))
        → 하지만 가독성이 나쁘므로 현재 풀이가 더 나음.

    [문제 풀이 흐름 적용]:
        1. 뭘 구해야 하는지  → X를 출력, X가 뭔지 파악이 핵심
        2. 입출력 확인       → 입력: T, S(문자열) / 출력: X(문자열)
        3. 제약 조건 확인    → S 길이 ≤ 20, 매우 작음 → 뭘 해도 됨
        4. 유형 분류         → 조건을 코드 언어로 번역하는 것이 핵심
           "X'가 S로 시작" → "X를 뒤집은 게 S로 시작" → "X가 reverse(S)로 끝남"
           모호한 조건을 구현 가능한 조건으로 변환하는 과정이 유형 분류의 핵심
        5. 생각하기 작성     → X = S + reverse(S)에서 겹치는 부분 최대화

    [어려웠던 부분]:
        1. "X'가 S로 시작한다"의 의미를 몰랐음
           → X'는 X를 뒤집은 것 → X'의 앞부분 = X의 뒷부분을 뒤집은 것
           → 결국 X는 reverse(S)로 끝나야 함
           → 핵심 스킬: 모호한 조건 → 코드로 바꿀 수 있는 조건으로 변환

        2. 겹치는 부분(overlap) 개념이 낯설었음
           → S끝 i글자 == R앞 i글자이면 겹치는 것
           → S.endswith(R[:i]) 로 확인
           → 최댓값을 갱신하면 자동으로 최대 겹침 길이가 남음

        3. sys.stdin.readline 사용 시 \n이 포함되어 잘못된 매칭 발생
           → input().strip() 으로 해결

        4. 바깥 for문과 안쪽 for문 변수명을 둘 다 i로 써서 충돌
           → 바깥은 i, 안쪽은 a로 구분

'''